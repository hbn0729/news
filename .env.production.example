# Production Environment Configuration Template
# 
# IMPORTANT: Copy this file to .env and fill in all REQUIRED values
# DO NOT commit .env file to git (it contains secrets)
#
# Usage:
#   1. cp .env.production.example .env
#   2. Edit .env and replace all CHANGE_ME_* placeholders
#   3. Run: bash scripts/validate-env.sh
#   4. Deploy: docker compose -f docker-compose.prod.yml up -d

# ============================================================================
# DATABASE CONFIGURATION (REQUIRED)
# ============================================================================

# PostgreSQL password - MUST be changed for production
# Generate strong password: openssl rand -base64 32
POSTGRES_PASSWORD=CHANGE_ME_GENERATE_STRONG_PASSWORD_MIN_16_CHARS

# ============================================================================
# SECURITY CONFIGURATION (REQUIRED)
# ============================================================================

# API secret key for protecting admin endpoints
# Generate: openssl rand -base64 32
# This key is required to call protected endpoints like /api/collect
API_SECRET_KEY=CHANGE_ME_GENERATE_STRONG_API_KEY_MIN_32_CHARS

# Allowed CORS origins (comma-separated)
# Examples:
#   - Empty string: No CORS (if frontend served by same nginx)
#   - Single origin: https://news.example.com
#   - Multiple: https://news.example.com,https://www.example.com
# Leave empty if frontend and backend are on same domain
ALLOWED_ORIGINS=

# ============================================================================
# PROXY SETTINGS (OPTIONAL - for accessing restricted news sources)
# ============================================================================

# HTTP/HTTPS proxy for accessing Google News or other restricted sources
# Format: http://host:port or http://user:pass@host:port
# Leave empty if no proxy needed
HTTP_PROXY=
HTTPS_PROXY=

# ============================================================================
# APPLICATION SETTINGS (OPTIONAL - defaults provided)
# ============================================================================

# Debug mode (set to false for production)
DEBUG=false

# News collection interval in seconds (default: 30 seconds for real-time sources)
COLLECTION_INTERVAL_SECONDS=30

# News retention period in days (older news auto-deleted)
NEWS_RETENTION_DAYS=7

# Timezone for cleanup scheduling (default: Asia/Shanghai)
# See: https://en.wikipedia.org/wiki/List_of_tz_database_time_zones
CLEANUP_TIMEZONE=Asia/Shanghai

# ============================================================================
# JIN10 API CREDENTIALS (OPTIONAL - defaults provided)
# ============================================================================

# Jin10 API credentials (defaults work but may rate limit)
# If you have your own credentials, set them here
JIN10_APP_ID=bVBF4FyRTn5NJF5n
JIN10_VERSION=1.0.0

# ============================================================================
# FRONTEND SETTINGS (OPTIONAL)
# ============================================================================

# Port to expose frontend on (default: 80)
# Change to 8080 or other if port 80 is already in use
FRONTEND_PORT=80
